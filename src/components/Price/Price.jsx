import PropTypes from "prop-types";
const Price = (option) => {
  console.log(option);
  return (
    <div>
      <h1></h1>
    </div>
  );
};
Price.PropTypes = {
  option: PropTypes.object,
};
export default Price;
